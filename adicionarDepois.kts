/*
qualquer tipo pode ser nulo porem isso deve ser explicitado na declaração da variavel atraves do (?)
var month:Int? = null
 */

fun main() {
    var a:Int = 1
    var b:Int = 2

    var c:Int = a.plus(b)
    println(c)
}